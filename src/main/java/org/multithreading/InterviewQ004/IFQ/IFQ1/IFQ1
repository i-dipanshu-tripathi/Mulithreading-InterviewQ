✅ 1. Can we call join() before start()?

Yes.

t1.join();
t1.start();

Since t1 has not started, there is nothing to wait for, so join() returns immediately.
However, this order is generally meaningless and should be avoided.

